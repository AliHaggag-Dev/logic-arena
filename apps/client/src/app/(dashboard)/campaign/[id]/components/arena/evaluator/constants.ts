export const ENERGY_COST: Record<string, number> = {
  FIRE: 8,
  MOVE: 2,
  MOVE_FAST: 4,
  BACKUP: 2,
  SCAN: 3,
  STOP: 0,
  BURST_FIRE: 18,
};

export const SCAN_RANGE = 0.55;
export const SCAN_FOV = 1.8;
export const MAX_OPS_PER_TICK = 20;
export const TWO_PI = Math.PI * 2;
